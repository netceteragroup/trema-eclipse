package com.netcetera.trema.eclipse.editors.xmleditor;

import org.eclipse.swt.graphics.RGB;



/** 
 * The classes of this package are based on the Eclipse PDE sample XML
 * text editor project.
 */
public final class IXMLColorConstants {
  
  /** Color for <code>XML_COMMENT</code>. */
  public static final RGB XML_COMMENT = new RGB(128, 0, 0);
  /** Color for <code>PROC_INSTR</code>. */
  public static final RGB PROC_INSTR = new RGB(128, 128, 128);
  /** Color for <code>STRING</code>. */
  public static final RGB STRING = new RGB(0, 128, 0);
  /** Color for <code>DEFAULT</code>. */
  public static final RGB DEFAULT = new RGB(0, 0, 0);
  /** Color for <code>TAG</code>. */
  public static final RGB TAG = new RGB(0, 0, 128);
  
  private IXMLColorConstants() {
    
  }
}
